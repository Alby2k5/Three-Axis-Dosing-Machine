# Generated Results

CSV result files are created by `scripts/run_all_analyses.m` and ignored by Git by default. Commit them only when they were generated in a documented MATLAB release and the assumptions/configuration are reviewed.
